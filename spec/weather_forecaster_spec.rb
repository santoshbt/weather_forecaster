require 'spec_helper'

describe WeatherForecaster do
  it 'has a version number' do
    expect(WeatherForecaster::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
